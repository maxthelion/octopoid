# Octopoid Configuration
# Generated by: octopoid init

# Mode: local (single machine) or remote (client-server)
mode: remote

# Server configuration (remote mode)
server:
  enabled: true
  url: https://octopoid-server.example.com
  cluster: production
  machine_id: machine-001
  # api_key: your-api-key-here  # Optional

# Agent configuration
agents:
  max_concurrent: 3
  # max_breakdown_depth: 1  # Maximum levels of task re-breakdown (default: 1)
  # models:
  #   implement: sonnet
  #   breakdown: sonnet
  #   gatekeeper: opus

# Repository
repo:
  path: .
  main_branch: main

# Hooks — lifecycle actions run during task processing
# Built-in hooks: rebase_on_main, create_pr, run_tests
# Default: before_submit: [create_pr]
# hooks:
#   before_submit:
#     - rebase_on_main
#     - create_pr

# Task type definitions — override hooks and agent assignment per type
# task_types:
#   product:
#     hooks:
#       before_submit:
#         - rebase_on_main
#         - run_tests
#         - create_pr
#   infrastructure:
#     hooks:
#       before_submit:
#         - rebase_on_main
#         - create_pr
#   hotfix:
#     hooks:
#       before_submit:
#         - run_tests
#         - create_pr

# Command whitelist for IDE permission systems (e.g., Claude Code)
# Declare commands that Octopoid agents need to run
# commands:
#   git:
#     - status
#     - fetch
#     - checkout
#     - branch
#     - commit
#     - push
#     - rebase
#     - merge
#     - diff
#     - log
#     - rev-list
#     - ls-remote
#     - worktree
#     - submodule
#
#   npm:
#     - run test
#     - run build
#     - run dev
#     - install
#
#   npx:
#     - vitest run
#     - tsc --noEmit
#
#   file_operations:
#     read:
#       - ".octopoid/**"
#       - "src/**"
#       - "tests/**"
#       - "package.json"
#       - "tsconfig.json"
#       - "*.config.*"
#       - "README.md"
#       - "docs/**"
#     write:
#       - ".octopoid/**"
#       - "src/**"
#       - "tests/**"
#       - "docs/**"
