# Octopoid Configuration
# Generated by: octopoid init

# Mode: local (single machine) or remote (client-server)
mode: remote

# Server configuration (remote mode)
server:
  enabled: true
  url: https://octopoid-server.example.com
  cluster: production
  machine_id: machine-001
  # api_key: your-api-key-here  # Optional

# Local database (local mode only)
# database:
#   path: state.db

# Agent configuration
agents:
  max_concurrent: 3
  # models:
  #   implement: sonnet
  #   breakdown: sonnet
  #   gatekeeper: opus

# Repository
repo:
  path: .
  main_branch: main
