title: API Key Management
description: >
  API keys are stored in .octopoid/.api_key (gitignored), not in tracked
  files. The env var OCTOPOID_API_KEY overrides the file. Key rotation
  is possible without code changes.

invariants: []
