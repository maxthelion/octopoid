title: Pull Request Lifecycle
description: >
  PRs are created by the create_pr flow step, reviewed by the gatekeeper,
  and merged by the merge_pr step. PR metadata (pr_number, pr_url) is
  stored on the task. Branches are never deleted on merge.

invariants: []
