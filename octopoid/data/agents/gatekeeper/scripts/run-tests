#!/usr/bin/env bash
# No-op: CI runs tests via GitHub Actions before the gatekeeper is invoked.
# The check_ci check in the flow system ensures CI has passed before this
# agent can claim a task. Running tests again here would be redundant.
exit 0
