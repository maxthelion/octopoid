#!/usr/bin/env python3
"""Move a claimed task back to incoming, clearing claim metadata.

This script previously unclaimed tasks via the local SQLite database.
That functionality has been replaced by the API.
"""

import sys

print("This script is not available in API mode. Use the API or SDK instead.")
sys.exit(1)
