#!/usr/bin/env python3
"""Review local submodule commits for an orchestrator specialist task.

Shows what an agent committed to the orchestrator submodule's main
branch that hasn't been pushed to origin yet.

Usage:
    orchestrator/venv/bin/python orchestrator/scripts/review-orchestrator-task <task-id>

Examples:
    review-orchestrator-task f2da3c14
    review-orchestrator-task TASK-f2da3c14
"""

import sys
from pathlib import Path

# Ensure the orchestrator package is importable
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from orchestrator.review_orch import main

if __name__ == "__main__":
    main()
